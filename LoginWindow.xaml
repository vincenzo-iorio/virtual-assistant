<Window x:Class="VirtualAssistant.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Width="300" Height="306"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" WindowStyle="ToolWindow">
    <Grid Margin="20,20,20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username" FontWeight="SemiBold"/>
        <TextBox x:Name="UsernameBox" Grid.Row="1" Margin="0,6,0,12"/>

        <TextBlock Text="Password" Grid.Row="2" FontWeight="SemiBold"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Margin="0,6,0,136"/>

        <Button Grid.Row="3" Content="Login" Height="32" Width="80" HorizontalAlignment="Center"
                Click="LoginButton_Click" Margin="0,58,0,10"/>
    </Grid>
</Window>